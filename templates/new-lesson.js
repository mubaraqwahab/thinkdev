const fs = require("fs")

// Get the title of the page

// Compute the order of the page

// Slugify the title of the page

// Create a lessons/*.md file with the slug
// and lessons.md template

// Create a slides/*.md file with the slug
// and slides.md template

// Indicate that you're done
