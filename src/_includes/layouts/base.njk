<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {# If page title is absent, show only site title #}
    <title>{{ title ~ ' | ' ~ site.title if title else site.title }}</title>
    <link rel="stylesheet" href="{{ 'css/main.css' | url }}" />
  </head>
  <body>
    {{ content | safe }}

    <script src="{{ 'js/bootstrap.bundle.min.js' | url }}"></script>
    {#
      Use the one below instead,
      if you're not using components that require Popper (like tooltips)
    #}
    {# <script src="{{ 'js/bootstrap.min.js' | url }}"></script> #}

    {# Optimize and inline JavaScript: https://www.11ty.dev/docs/quicktips/inline-js/ #}
    {% set js %}
      {% include "js/index.js" %}
    {% endset %}

    {% if site.environment == "production" %}
      <script>{{ js | transpilejs | minifyjs | safe }}</script>
    {% else %}
      <script>{{ js | safe }}</script>
    {% endif %}
  </body>
</html>
