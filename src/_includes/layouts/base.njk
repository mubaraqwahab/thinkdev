<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title ~ ' | ' ~ site.title if title else site.title }}</title>
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}" />
  </head>
  <body>
    <header class="bg-light text-dark py-2 border-bottom">
      <div class="container">
        <a class="text-reset text-decoration-none fs-5" href="{{ '/' | url }}">Navbar</a>
      </div>
    </header>

    {{ content | safe }}

    <script src="{{ '/js/bootstrap.min.js' | url }}"></script>

    {# Optimize and inline JavaScript: https://www.11ty.dev/docs/quicktips/inline-js/ #}
    {% set js %}
      {% include "js/index.js" %}
    {% endset %}

    {% if site.environment == "production" %}
      <script>{{ js | transpilejs | minifyjs | safe }}</script>
    {% else %}
      <script>{{ js | safe }}</script>
    {% endif %}
  </body>
</html>
